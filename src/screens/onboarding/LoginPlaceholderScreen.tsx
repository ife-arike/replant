// KAN-38 — full implementation lives in LoginScreen.tsx. This file
// kept as a re-export so OnboardingNavigator's existing import path
// continues to resolve without a navigator change.
export { default } from './LoginScreen';
